<head>
<title>Lascivia</title>
<link rel="app/assets/stylesheets/pages/_home.scss" href="">
</head>

<body>
  <div class="banner">
    <h1>Lascivia</h1>
    <%= render 'partials/searchbar' %>
  </div>
  <div class="container">
    <!--
    <div class="carousel-categories" data-flickity='{ "groupCells": true }'>
      <% Category.all.each do |category| %>
      <div class="category">
        <%= category.name %>
      </div>
      <% end %>
    </div>
      -->

      <button class="btn-warning" onclick="toogleChat()">clica aqui</button>

    <div class="carousel-products" data-flickity='{ "groupCells": true }'>
      <% Product.all.each do |product| %>
        <div class="carousel-cell">
            <%= link_to product_path(product) do %>
              <% if product.photo.present? %>
                <%= image_tag product.photo, id: "photo" %>
              <% else %>
                <%= image_tag "" %>
              <% end %>
            <% end %>
            <hr class="line">
            <div class="title">
              <h3><%= product.name.upcase %></h3>
            </div>
            <hr class="line">
            <div class="infos">
              <p><%= product.category.name %></p>
              <p>R$ <%= product.price %></p>
            </div>
        </div>
      <% end %>
    </div>
  </div>
</body>
